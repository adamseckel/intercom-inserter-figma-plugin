import { default as button } from './Button';
import { default as radioButtonGroup } from './RadioButtonGroup';
import { default as tabControl } from './TabControl';
import { default as tab } from './Tab';
import { default as dropdownItem } from './DropdownItem';
import { default as dropdownGroup } from './DropdownGroup';
import { default as select } from './Select';

const Core = {
  button,
  radioButtonGroup,
  tabControl,
  tab,
  dropdownItem,
  dropdownGroup,
  select,
};

export default Core;
